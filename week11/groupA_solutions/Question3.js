// Question 3

let x = prompt("Enter a number");

function doubleNumber(x){
	return x * 2;
}

let result = doubleNumber(x);

document.write(x + " * 2 = " + result);
 /*
 Create a function named doubleUp(x). This function can be passed a single value, x. The function should multiply this value by 2 and return the result. Use a prompt to ask a user to enter a number. Pass this number to the function, and then display the result returned by this function on the web page. See figure 3 below for examp
 */