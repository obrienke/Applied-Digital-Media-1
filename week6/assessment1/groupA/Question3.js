/* Question 3 */

let one = +prompt("Enter first number");
let two = prompt("Enter second number");
let three = prompt("Enter third number");

alert(one + " + (" + two + "/" + three + ") = " + one 
		+ " + " + (two / three) + " = " + (one + (two / three)));
